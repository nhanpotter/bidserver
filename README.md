# bid-server

```shell
# Go to project root
# Create virtual env
python3 -m venv .venv
# Activate venv
source .venv/bin/activate
# Install dependencies
pip3 install -r requirements.txt
# Migrate
./manage.py migrate
```